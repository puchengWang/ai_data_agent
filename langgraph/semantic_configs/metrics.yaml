metrics:
  user_count:
    business_name: 用户数量
    description: 统计用户数量
    table: users
    aggregation: count
    field: rid
    measure: COUNT(rid)
    time_field: atime
    metric_type: normal
    dimensions: 
      - level
    filters: []
    meta:
      engine: aurora_mysql
      datasource: user
      database: user
      table: users