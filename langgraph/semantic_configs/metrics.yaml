metrics:
  user_count:
    business_name: 用户表数量
    description: 统计 用户表 的数量
    table: users
    measure: COUNT(rid)
    time_field: atime
    metric_type: count_by_time
    dimensions: []
    filters: []
    meta:
      engine: aurora_mysql
      datasource: user
      database: user
      table: users
